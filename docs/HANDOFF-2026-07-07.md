# Session handoff — 2026-07-07 (M8 landing + staging activation)

Point the next agent here. This file is the authoritative state; it supersedes the
account-memory M8 notes. Protocol: docs/ORCHESTRATION.md + docs/SUBAGENTS.md (Claude
architects/reviews, codex implements via `codex exec`, codex can't git-commit).

## Where things stand

**All of M8 is merged to main, deployed to staging, and activated.** 284 tests green.

- M8-01..06 done (status lines flipped). M8-04 (creation wizard) landed this session:
  commit `484e1de` on task/M8-04, merged as `f1fa11a`. Codex implemented; architect
  review passed all constraints (plain-SQL migration 0020, kernel emitEvent for all
  intake.* events, provisionScope called exactly once post-approval, no LLM in wizard
  critical path, MCP subtree denial tested). Architect removed codex's tsconfig/vitest
  alias workarounds and validated its hand-edited pnpm-lock against real pnpm.
- **Deploy pipeline root cause found + fixed**: the release workflow never synced the
  compose file to the VPS — staging ran a hand-placed stale copy (plain postgres:17,
  no pgvector → migration 0018 died on CREATE EXTENSION vector). PR #1 (merged by
  owner) makes deploy-staging scp `infra/docker-compose.prod.yml`, `postgres-init.sql`,
  `litellm.config.yaml` to `~/app/` before compose up. **The repo is now source of
  truth for the staging stack.** PR #2 (merged) added GITHUB_ORG/GITHUB_TOKEN/
  SKILLS_REPO passthrough to the os container.
- Staging (cos-staging.risi.au) deploy for `96427c2`: release + deploy-staging both
  green, migrate success, smoke passed.

## Activation completed this session (guided owner walkthrough, all verified)

1. **LiteLLM virtual keys**: `LITELLM_EMBED_KEY` + `BRAIN_LITELLM_API_KEY` minted via
   /key/generate inside the litellm container (aliases `companyos-embed`,
   `companyos-brain`), written to `~/app/.env`, os recreated, env-ok verified.
2. **Skills repo**: created https://github.com/Brissie-Digital-PTY-LTD/companyos-skills
   (private). Seeded: `wiki-maintenance/SKILL.md` (brain engine system prompt, distilled
   from docs/patterns/WIKI.md), `scope-intake/SKILL.md` + 3 templates (byte-matched to
   DEFAULT_TEMPLATE_FILES in packages/api/src/modules/intake/service.ts), README.
   First `sync_skills` run succeeded: `added: 2`.
3. **GITHUB_WEBHOOK_SECRET**: owner set + tested (org webhook deliveries 200).
4. **Brain cron**: cos_ token named `brain-engine` minted in root Connect UI (agent
   role, 90 days), elevated to root admin via SQL UPDATE on grants (Connect UI can only
   mint agent/viewer; sync_skills + engine need admin). `BRAIN_ENGINE_TOKEN` +
   `COMPOSE_PROFILES=brain` in `.env`; `companyos-brain-cron-prod` running (ingest
   02:00 daily, lint 03:00 Sun). Compose reads COMPOSE_PROFILES from the env file —
   verified working.

## ⚠️ Credential expiries — both ~2026-10-05 (90 days)

- `brain-engine` cos_ token (BRAIN_ENGINE_TOKEN in staging .env)
- GitHub fine-grained PAT `companyos-skills-sync` (GITHUB_TOKEN in staging .env,
  contents r/w on companyos-skills only)

Silent-death risk documented in docs/tasks/M9-01-ops-health-panel.md (new scope, todo).
Rotate both at live promotion regardless.

## IN FLIGHT — next immediate step (step 9, provider keys probe)

The virtual keys authenticate into LiteLLM, but providers need real keys:
`infra/litellm.config.yaml` maps cheap/analysis/reasoning/code → Moonshot Kimi
(MOONSHOT_API_KEY) and `embed` → os.environ/LITELLM_EMBED_MODEL (default
openai/text-embedding-3-small) via **OPENAI_API_KEY**. The app requests alias `embed`
(packages/api/src/lib/embeddings.ts); brain requests `cheap`/`analysis`.

A probe block was given to the owner to run on the VPS (checks which provider key
names exist in .env, then live-fires one chat call with BRAIN_LITELLM_API_KEY and one
embeddings call with LITELLM_EMBED_KEY through localhost:4000). **Its output was never
seen — session ended. Re-run it first.** Expected: Moonshot OK (was the only live
provider key as of early July), embed likely FAIL until OPENAI_API_KEY is added to
.env (litellm must be recreated after: `docker compose ... up -d litellm` — config/env
are read at startup). Without OPENAI_API_KEY: semantic search stays lexical-only,
fail-open, everything else works.

## Then: remaining sequence

1. **Brain smoke**: as root admin open /brain/engine on staging, trigger a manual
   ingest run — should complete green now (key + skill + admin principal all in place)
   and write first wiki pages. If it errors, check run report + litellm logs.
2. **M8 owner walkthrough** (suggested path): create test project → wizard framing →
   external pack → paste-back → review → approve → provision (separate act) →
   /admin/intake; hybrid search + [[wikilinks]]; /brain graph + Ask OS; recall_memory/
   get_context via MCP; template editor round-trip.
3. **Tag v0.8.0** after owner sign-off (semver v* tag = live-promotion artifact;
   COMPOSE_TAG=main is staging-only). Owner decision.

## Pending backlog (docs/tasks/*.md status lines are authoritative)

- **M9-01 ops health panel** (todo, scoped this session): credential expiry warnings,
  job liveness, run log surface, email alerts. Born from this session's 90-day tokens.
- **M5-04 tenant admin** (draft, owner decisions pending): now includes an explicit
  "LLM & keys admin" section (owner request 2026-07-07) — virtual key mint/revoke/
  budgets, alias/provider-key status, spend dashboards. Cross-refs M7-03 (/admin/mcp,
  done) and M9-01. Don't create a separate LLM-admin scope — it lives here.
- **M5-05 control plane v1** (draft, owner decisions pending).
- **M5-03 backups/DR** (blocked on offsite creds from owner).

## Uncommitted/unpushed state in this repo

- Local commits on main awaiting push (classifier blocks agent pushes to main in this
  environment — even docs; use branch+PR or have the owner run `git push origin main`):
  `3e8a326` (M9-01 scope doc) + whatever commit carries this handoff and the M5-04
  addendum. Nothing else dirty.
- Owner runs one-liners via `!` prefix in the session; that's how PR merges and repo
  creation were done (classifier requires human action for org repo creation, deploy-
  pipeline changes, and main pushes).

## Operational facts (verified this session)

- VPS: stack at `~/app`, podman with docker-compose provider, `docker compose
  --env-file .env -f docker-compose.prod.yml ...`. Postgres user from .env
  POSTGRES_USER; DBs companyos/plane/litellm; postgres now pgvector/pgvector:pg17
  (same major as before — data volume preserved).
- .env keys added this session: LITELLM_EMBED_KEY, BRAIN_LITELLM_API_KEY, GITHUB_ORG
  (Brissie-Digital-PTY-LTD), SKILLS_REPO (companyos-skills), GITHUB_TOKEN (PAT),
  BRAIN_ENGINE_TOKEN, COMPOSE_PROFILES=brain. GITHUB_WEBHOOK_SECRET set by owner.
- MCP over HTTP: https://cos-staging.risi.au/api/mcp, stateless streamable, Bearer
  cos_ token. Recipe for calling tools from the workstation (pnpm layout — script must
  live under packages/mcp/node_modules/ for SDK resolution, root node_modules won't
  resolve):

  ```js
  // packages/mcp/node_modules/.tool-call.mjs ; run: COS_TOKEN=cos_... node <path>
  import { Client } from "@modelcontextprotocol/sdk/client/index.js";
  import { StreamableHTTPClientTransport } from "@modelcontextprotocol/sdk/client/streamableHttp.js";
  const t = new StreamableHTTPClientTransport(new URL("https://cos-staging.risi.au/api/mcp"),
    { requestInit: { headers: { Authorization: `Bearer ${process.env.COS_TOKEN}` } } });
  const c = new Client({ name: "cli", version: "1.0.0" });
  await c.connect(t);
  console.log(JSON.stringify(await c.callTool({ name: "sync_skills", arguments: {} }), null, 2));
  await c.close();
  ```

- Codex: ChatGPT-login auth, refilled 2026-07-07 (~99%). Dispatch pattern in
  docs/SUBAGENTS.md; include LIMIT-ALERT instruction; architect always re-runs
  pnpm typecheck/lint/test (codex uses repo-local binaries, can't run pnpm).
- Migrations: plain SQL only (no DO $$ / nested dollar-quoting — drizzle-kit CLI dies
  silently; packages/db/scripts/migrate.mjs is the migrate path). New workspace
  packages need COPY lines in apps/os/Dockerfile (wizard already has them).
- Windows dev quirks: MSYS_NO_PATHCONV=1 for docker exec; clean apps/os/.next after
  branch switches.
